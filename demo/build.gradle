plugins {
    id 'java'
}

group 'com.epam'
version '1.0.0'

repositories {
    mavenLocal()
}

dependencies {
    implementation 'com.epam:utils:1.0.0'
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')