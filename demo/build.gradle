plugins {
    id 'java'
}

group 'com.epam'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation files('../utils/build/libs/utils.jar')
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')